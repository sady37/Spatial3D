# 新会话提示词

> 把下面整段(从"===" 到结尾)粘进一个新的 Claude Code 会话即可无缝接续。

===

我在继续 **Spatial3D** 项目(养老跌倒监测 V3.0 的核心:用固定安装的 TI AWRL6844 60GHz 毫米波雷达,
长期累积建立高清 3D 静态空间模型,作为跌倒等检测的基础)。工作目录 `/Users/sady3721/project/owl/Spatial3D`。

**请先读这两份文件再动手**:
- `docs/DESIGN-2026-07-05-静态3D与跌倒检测.md` —— 已达成的全部设计结论(务必读,别重复推导)
- `docs/GET-DATA-FROM-TI.md` —— 数据链路怎么跑通的

**当前状态(已跑通,别回头重做)**:
- 雷达固件已永久烧入 flash;数据可在 Mac 直读:**DATA 口 `/dev/cu.usbmodem0000RA444` @ 1250000 baud**
- `pc/` 是 Python 3.12 venv(`pc/.venv`),`pc/spatial3d/tlv.py` 解析器已用真实数据验证通过
- 实时查看真实帧:`cd pc && .venv/bin/python -m spatial3d.dump --data-port /dev/cu.usbmodem0000RA444 --baud 1250000`
- Ubuntu VM(装了 SDK,可 SSH:`ssh -p 2222 -i ~/.ssh/ti_vm_ed25519 sady@127.0.0.1`,免密 sudo)默认关机,
  需要时 `VBoxManage startvm <UbuntuUUID> --type headless`;Win10 VM 用于烧录+visualizer

**核心共识(见设计文档,勿再动摇)**:
1. 系统 = 耐心的 3D 静态变化监测器;**延迟不敏感**(30s/300s 无差别)→ 为置信度优化,不为速度
2. 静态图 A 本身是目标,跌倒 = "相对 A 的持续贴地(≤40cm)新质量";A 判别位置(床 vs 地面),时间给置信度
3. **A 必须尽量高清**(A 的清晰度封顶系统精度)——超分辨对 A 是核心非可选;因 A 离线慢建,重武器都用得起
4. 硬件天花板:4×4 虚拟阵,~25°波束,当前误差 50~60cm;超分辨+累积可压到 ~20-30cm,到不了厘米级(距离维才厘米)

**下一步任务(第一阶段)**:在 `pc/` 里搭"耐心监测器"骨架 ——
Map A 累积器(log-odds/能量,慢时间常数)+ 分钟级当前图 + B−A 差分 + 一个"A 清晰度"量化指标。
不要实时 tracker、不要触发器。用现在流进来的真实点云跑通"人躺地上几分钟被稳定标出"。
之后第二阶段再攻高清 A(改固件吐复数 → PC 端 IAA/MUSIC/稀疏 → 融合走圈轨迹 → 多夜累积)。

请先读上面两份文档,然后我们从第一阶段开始。
