# Python
pc/.venv/
__pycache__/
*.pyc
.pytest_cache/

# DSP build output
dsp/build/

# OS / editor
.DS_Store
xwrl68xx_xds110.ccxml

# raw sleep-pad session data (177MB, kept local)
sleepad/

# radar captures + run logs.
#   pc/record/  = RAW recordings (incl. live 5-min files). NOT uploaded.
#   pc/case/    = curated cubes the CURRENT algorithm uses. UPLOADED (whitelisted below).
pc/*.log
# ignore all cubes by default...
pc/**/*.npz
# ...except the curated algorithm case set
!pc/case/*.npz
# raw dir never uploaded (cubes, csv, anything)
pc/record/
