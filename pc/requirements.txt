# PC-layer (spatial modeling) dependencies.
# Target interpreter: Python 3.12 (Open3D has no 3.14 wheels yet).
numpy>=1.26,<2.2
scipy>=1.11
scikit-learn>=1.4          # DBSCAN clustering
open3d>=0.18               # point cloud, RANSAC plane fit, ICP, visualization
pyserial>=3.5              # UART voxel-map sync

# Dev / debug
pytest>=8.0
